# BCReview--001 — whole-set review candidate

State: DRAFT; independent exact-HEAD review pending.
Authority: #55/#21; accepted design merge4a42222f7bfedc1d80693effbb25a1a82fcff65e.
Candidate: seven BCs1.0.0 plus BoundaryTypes--001 and bounded specialization evidence.
No product implementation, frozen contract or resolved product finding is claimed.

Master owns shared values, TUI/Application, State/View and Discovery contracts.
Separate bounded authors produced Git/GitHub and Runtime/Persistence drafts; Master
reconciled actual files and their shared references. An independent whole-set
reviewer must now inspect the frozen committed candidate, not author summaries.

Important specializations to review:

- Public opaque source/storage versions live in API with optional ports aliases,
  preserving API->Domain and prohibiting API->ports cycles. Private Git plan/
  continuation receipt wrappers stay ports-owned.
- Compound workflows stay Application-owned. Git advances only verified authorized
  predecessor facets under the original plan/choice; fresh current state cannot
  become implicit renewed approval.
- Commit uses the native private commit engine with real-context hooks, candidate
  index, native HEAD-first prepared transaction and protected index publication.
  Later message-hook staging is distinct from the commit tree.25 native temporary
  cases passed; the signer fixture proves invocation, not cryptographic validity.
  BC55-GIT-01 is proposed resolved pending this independent review.
- Missing worktrees retain identity without fabricated acquired Scope/Head.
  App owns canonical relations and active ContextVersion; stale queries cannot
  reverse newer context, and Active deploy binds its expected context version.
- Invocation distinguishes literal argv from explicit interactive-shell policy.
  Runtime owns shell ancestry and the reviewed Windows batch carrier needed by
  ordinary package-manager shims. A native temporary carrier fixture passed exact
  colon/whitespace/Unicode/metachar/trailing-slash/empty argument cases; unsupported
  quote/percent/control grammar refuses rather than retargets. Full real package-
  manager/Runtime integration remains required.
- Runtime's accepted native brokers/supervisors and downstream cleanup reservation/
  transfer-before-ACK remain intact. Storage preserves exact commit points and
  version1/unknown-member semantics. State/View owns no backend workflow or I/O.

Master structural checks: exactly seven BCs and one shared annex, all DRAFT1.0.0;
balanced Go fences and existing local Markdown link targets. A first naive link
regex treated generic `Some[T](value)` inside a Go fence as a link; excluding fenced
code corrected that checker, not the contract. Full tracked whitespace/scope/hash
checks accompany the frozen candidate. Accepted design and product code remain
unchanged. All143 product findings remain open.

Review must cover concrete method/type/result correspondence, error/effect and
option/tag semantics, cross-source identity, resource and continuation lifetime,
immutable copying, admission/ACK/shutdown, real retained capability, complete
verification references and consistency with accepted design. Record findings,
correct and re-review exact SHA before any FROZEN1.0.0 metadata or implementation.
