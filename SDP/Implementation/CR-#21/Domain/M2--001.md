# M2 Domain foundation worker report

State: IMPLEMENTED — candidate for separate fresh independent review.
Authority: #58 under full parent #21; Sprint-004-v04 / I-03 / M2.
Role: fresh Domain Worker, m2_domain_worker. Date: 2026-09-06.
Branch: codereview-21/layer-domain.
Worktree: C:/Users/hanse/GIT/gh-tree-wt/domain-implementation.
Kickoff HEAD: 75a214200d1ab192f491e11c7165e1d5def4712d.
Frozen candidate is the commit containing this report; Master records its exact
source/review/integration SHAs separately. No self-review acceptance is claimed.

## Authority and actual contribution

Read AGENTS.md and developmentInstructions completely, full #58/#21 and comments,
the corrected accepted Domain LR--001, accepted REFDES/API A1, migration/Slice/
verification/finding maps, current sprint/traceability, M2/Plan and frozen
BoundaryTypes plus Domain consumers across all seven BCs. BCFreeze--001 establishes
effective FROZEN1.0.0 from97cc0d8257603766dd741b49b7d8005857b421a9. M1 predecessor
source/review/integration05982a4 remains accepted. No contract change needed.

Only new internal/domain files and this bounded report change. No legacy source
or test, other layer, module/workflow, accepted design/BC or program metadata is
edited/moved/deleted. The CLI still uses the baseline stack. All SLC-01..13 remain
selected; this shared M2 foundation completes no vertical Slice or baseline
finding. All143 baseline product finding IDs remain open pending their full proof.

Implemented comparable private-field RepositoryID, WorktreeID, BranchID, PRID,
OID/ObjectFormat, Revision, Head, StashID, LaunchPointID, SessionID and ExactTarget.
Every identity/variant zero is invalid. Constructors return invalid zero/error
on failure; Equal matches Go ==. No mutable backing or open interface variant
crosses the surface. Head and ExactTarget are validated private tagged structs,
explicitly permitted by frozen BoundaryTypes B1. Named kind constants denote
Attached/Detached/Unborn and CommitTarget/BranchTarget/PullRequestTarget; callers
cannot submit a discriminator plus contradictory optional payloads.

Local and Remote scopes are distinct. Local/RemoteHead BranchID require matching
LocalCommon/Remote repositories. Attached branch/revision scopes agree; all Head
variants are local. Unborn has no Revision. Head.MatchesWorktree checks local
common scope, leaving actual observed-worktree binding to Git/API. PRID belongs
to its base remote repository; expected head retains its own explicit remote
scope, permitting forks. Application still verifies actual endpoint association.
Targets always retain expected exact Revision, with no latest-ref substitution.

The exact branch suffix grammar is documented in internal/domain/README.md against
[the primary Git reference](https://git-scm.com/docs/git-check-ref-format), read
2026-09-06. It preserves literal bytes below refs/heads/, including -topic, HEAD,
@ and prefix-looking names, while rejecting invalid stored ref syntax and @{-n}.
Native creation/shorthand restrictions are adapter-owned. No native Git executes
in Domain tests. Master's separate native reference check is not counted as a
Domain unit test or future Git adapter proof.

LaunchPointID encodes decimal byte-length:component for provider/project/member;
root project may be empty. It does not include label, saved alias, content/source
version or executable override. No raw encoded-key constructor bypasses the rule.
Repository/admin/project tokens retain bytes; their canonicalization is adapter
work. Runtime alone owns SessionID allocation, no reuse and wrap refusal. Domain
imports only errors, strings, strconv and encoding/hex and performs no I/O,
environment/context/clock/path/URL/process/transport/storage/serialization work.

## Local verification and evidence limits

Native host: Windows/amd64. Go: go1.25.0 from
C:/Users/hanse/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.0.windows-amd64/bin;
its bin was added only to each command environment. Git2.43.0.windows.1;
gh2.96.0 (2026-07-02). No installed extension/user configuration changed.

| Command / case | Actual result |
|---|---|
| gofmt -w internal/domain, followed by gofmt -l check | Formatted new files only; no remaining formatting output. |
| go test ./internal/domain -count=1 -cover | PASS, 100.0% statement coverage. Coverage is not independent acceptance. |
| go test -race ./internal/domain -count=1 | PASS, native Windows/amd64. |
| go test ./... -count=1 | PASS including unchanged baseline tests and M1 architecture tests. |
| go vet ./... | PASS. |
| go build ./... | PASS. |
| go run ./internal/composition/architecture | PASS all12 target selections and public types;61 exact legacy/shared-entry allowances used. No checker change/suppression. |
| Compiler overlay positive control | Valid public RepositoryID construction/access compiles, exit0. |
| Compiler overlay negative cases | All six fail with expected specific diagnostics, exit1: private Head fields, embedding ExactTarget as a new variant, forged RepositoryID fields, nil Head, scalar SessionID cast, assignment to Token getter. |

The first local test compile found missing explicit types in nested test composite
literals. Corrected these test syntax errors, then ran the passing commands above.
No product/checker failure or scope conflict was hidden. Strict final-tree mode
is intentionally not an M2 acceptance command: legacy remains until M7.

Deterministic tests cover V-DOM-01/02 value evidence: scope/kind mismatches, clone
versus linked administrative identities, exact40/64 OID case/nonhex/null/whitespace/
abbreviation, no fabricated unborn Revision, all Head/target alternatives, fork
base/head preservation, expected-tip drift, byte-length/delimiter/concatenation
launch collisions and nested copy isolation. External reflection plus compiler
constraints require private comparable value fields and no open/mutable type
representation; package-local adversarial cases reject contradictory tags.

Compiler probes used go test -overlay <fixture.json> ./internal/domain -run '^$'
with an overlay of surface_test.go only, leaving candidate source unchanged.
The positive control and each expected diagnostic prevent unrelated compilation
failure being counted as proof. Temporary raw snippets/overlays/logs are retained
for reviewer reproduction at:
C:/Users/hanse/AppData/Local/Temp/gh-tree-m2-domain-b4c11b0005204fbebdaf6bd38c66e1e5.
They are supplemental worker evidence, not an independent review artifact.

V-DOM-03 has Domain purity/copy evidence here. Runtime registry allocation,
concurrency/exhaustion/no-reuse and native Git object/ref/worktree identity proofs
remain their later layer contracts. Cross-target architecture/export checks do
not establish native Runtime/platform behavior or end-to-end capability.

## Frozen handoff and next permitted action

Commit/push the candidate and verify configured CI at that exact branch HEAD;
CI is not started at the instant this report is authored. Actual run URL/job
status and exact frozen HEAD are sent to Master after push, with no success
inferred before completion. A fresh separate reviewer must open the actual
source/tests/authority and report ACCEPT or findings at that exact SHA. Worker
then stops authoring; corrections require explicit re-review. Master alone
integrates after acceptance/current configured branch CI and updates sprint,
index/relations/ledger/verification evidence. Subsequent API/ports and viewmodel
Issues remain gated on reviewed Domain integration. No product PR, closure,
tag, publication, installed-extension update or Slice completion is claimed.
