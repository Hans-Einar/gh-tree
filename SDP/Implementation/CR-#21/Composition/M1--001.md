# Composition M1 implementation checkpoint

State: WORKER COMPLETE / AWAITING INDEPENDENT REVIEW AND EXACT BRANCH CI.
Authority: Issue #57 under #21; Plan--001, accepted #52 design and seven FROZEN
1.0.0 BCs/shared annex under #55. Sprint-004-v04 / I-03 / M1; all SLC-01..13
remain selected. This contribution completes no Slice or baseline finding.

Worker branch: `codereview-21/layer-composition-bootstrap`.
Worktree: `C:/Users/hanse/GIT/gh-tree-wt/composition-bootstrap`.
Kickoff: `4c0cee9f6693c4f0ab58a16a7e98299a67a0f76b`.
Master-only metadata subsequently fast-forwarded through
`47ad595703b2d779dd21d62630d47a5683b754c6`; no worker edit to those files.
Implementation commits: `6160a50e023033e7e4f9db85d1d3224306819876` and
`8008073c5c2ade0bf893e814247808659a782c0f`. The latter is the exact code source;
this report's commit is its documentation-only frozen-review successor. The
worker supplies that final full SHA and actual CI results to Master at handoff.

## Bounded output

Only delegated files changed: `.github/workflows/ci.yml`, `release.yml`, new
`internal/composition/architecture/{main.go,policy.go,check.go,check_test.go,
testdata/cases.json,README.md}`, and this report. No module/dependency, old product
source/test, accepted design, frozen BC, public asset, user configuration or
installed extension changed. No new BC or generic eleventh layer was introduced.

CI retains main/codex push and PR runs, adds canonical codereview-21 pushes and
manual dispatch, preserves native test/vet/build plus Linux race, and adds native
windows-11-arm with actual image/OS/Go/Git/gh/module logging and Go/host architecture
assertions. Twelve build/architecture jobs use the exact accepted pair/name table;
the dynamic matrix generator compares against Verification--001 and rejects any
omitted, extra, duplicate or changed target. Cross-builds use CGO disabled and
explicit default microarchitecture values.

The Go checker enforces exact module-qualified dependency roles, pure-layer
imports/symbols/syntax, private subpackage ownership, exported type graphs and
separate test-import policy across selected source/export data for every target.
Aliases, nested/local types, methods, generic values and inferred variables are
checked. API's narrow json.Valid exception implements the frozen Storage BC's
OpaqueJSON invariant without moving its schema encoding out of Persistence.

Staged allowances are the 60 exact old paths/blobs in MigrationMap plus the
separately classified unchanged CLI entry. LF-canonical hashing matches actual
Git blobs on this CRLF-capable checkout. New callers cannot import old backends;
changed/renamed/new legacy paths fail. Strict mode has no exemptions and requires
the complete final package inventory, including the accepted Runtime broker/helper
layout. Semantic ownership and immutable/pure behavior still need source review.

The Runtime prerequisite hook reports `pending-m3` with **conformance NOT RUN**
until Runtime source/image inputs exist. Any Runtime Go/image input requires Go
source in broker, broker/cmd and cmd/helpergen plus nonempty brokerassets inputs
`broker-amd64.gz`, `broker-arm64.gz`, `manifest.json`. Once ready, CI requires
`go run ./internal/runtime/cmd/helpergen -check` on native Windows/amd64 Go1.25.0,
then rejects checkout changes and checks ordinary committed-input builds. Runtime
M3 must implement the real independent source-closure/image/PE/compression/manifest
rebuild verifier described in WindowsBroker--001. No stub, helper generation or
native conformance is supplied or claimed by M1.

Release workflow now has only manual compilation staging and contents:read. It
verifies a supplied full source SHA, builds all twelve exact names, writes a
source/workflow/toolchain/length/SHA256 manifest and uploads a workflow artifact.
It cannot create tags/releases or clobber published assets. Its manifest explicitly
states `compilation_only_M8_not_verified`; publication and complete artifact/native/
helper/install-upgrade proof require the later independently reviewed M8 work.

## Executed evidence

Local environment: Windows amd64, Git 2.43.0.windows.1, gh 2.96.0; direct Go1.25.0
from `C:/Users/hanse/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.0.windows-amd64/bin`.
Temporary fixture modules use Go testing's private temporary directories. Temporary
cross-build output remains at
`C:/Users/hanse/AppData/Local/Temp/gh-tree-m1-builds-ffdbd8708b0549c9a788cd2cac3f3ec1`.

| Command / observation | Actual result |
|---|---|
| `go test ./internal/composition/architecture -count=1` | PASS after corrections, including final code source8008073; 29 compiled JSON policy scenarios, exact legacy/CLI/strict inventory checks, real unchanged-legacy caller rejection, Runtime prerequisite and OpaqueJSON/callback/recursive-type cases. |
| `go test ./...` | PASS on native Windows; existing worktree suite completed in25.653s. Run preceded the final narrow OpaqueJSON predicate addition; final exact branch CI repeats the complete suite. |
| `go vet ./...` and ordinary `go build -trimpath ... ./cmd/gh-tree` | PASS native Windows in the same full-suite command; final checker-only vet repeated after the predicate addition and passed. |
| `go run ./internal/composition/architecture` | PASS for all12 actual target source/export selections;61 exact staged allowances. Repeated after public-type/test-import hardening, before the final narrow OpaqueJSON predicate addition. |
| CGO_ENABLED=0, each accepted GOOS/GOARCH, `go build -trimpath -o TEMP/ASSET ./cmd/gh-tree` | All12 actual executable builds PASS, all exact accepted names. Compile evidence only. |
| `go run ./internal/composition/architecture -targets` | Exactly12 accepted pairs/names; independent accepted-table comparison passed. |
| `go run ./internal/composition/architecture -runtime-prerequisite` | `pending-m3`; explicit NOT RUN diagnostic. No Runtime inputs exist. |
| `go run ./internal/composition/architecture -mode strict` | Expected nonzero: remaining `internal/app/service.go` requires empty legacy allowance. No false final conformance. |
| `go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.11 -shellcheck= -pyflakes= .github/workflows/ci.yml .github/workflows/release.yml` | PASS workflow/YAML/expression checks. External shellcheck/pyflakes were disabled; Go fetched the pinned linter into its cache, no go.mod/go.sum change. |
| gofmt, staged `git diff --check` and owned-file scope | PASS; Git emitted only expected LF-to-CRLF checkout notices. |

Initial fixture run deliberately demanded an exported-type error for an inferred
time.Timer. It failed because the independent symbol check rejected that forbidden
type first. Public-type traversal was moved before symbol checks, and the same
fixture now proves the intended exported-type rejection. No failing case was
deleted, converted to a pass or hidden behind a build error. Later hardening added
exact GOROOT test-import resolution, recursive-type termination and callback
argument rejection. Final tests/vet pass. No product behavior failure was observed.

Native Linux/macOS/Windows ARM64, Linux race and the final12 build/check jobs are
pending branch CI at this report commit. The worker will report actual final
run/job state and exact SHA; Master records accepted review/integration and those
results in the verification/traceability surface. The intentionally skipped M3
helper conformance job is distinct from the successful prerequisite classification.
No native Runtime/ABI/Slice/M8 release proof is claimed from baseline tests.

## Review and next permitted action

Freeze/push the final report successor and stop authoring. A separate fresh reviewer
must inspect that exact source/tests/workflow/authority set, run meaningful negative
cases and return ACCEPT or findings. The README contains every reproducible checker
entrypoint, the separate policy and M3 command contract. Corrections require another
frozen SHA/re-review. Master alone integrates after review and all configured
applicable branch CI passes, records the exact SHAs/evidence, and authorizes M2.
No product PR/main integration, legacy retirement, tag or publication is authorized
by this checkpoint. All143 baseline findings remain open.
