# M3 Git contribution — Issue #61

State: PARTIAL CHECKPOINT; no adapter acceptance or completed Slice.
Worker branch: codereview-21/layer-git; base412f33e477cec03cb6eafe7b846c9bcdd02c0a25.
Authority: #21/#61 resumed policy, M3-Adapters--001, accepted M2 source and
Application--Git FROZEN1.1.0/BoundaryTypes FROZEN1.0.0. Master dispatch73a4585/ledger57.
Owned: internal/git/ and this bounded report only. CLI still uses legacy code.

## Current handoff — checkpoint11 / HOLD

Technical source8c2584bff7c1f04461cd2f519bc3f6d5921360db is pushed. Its CI
run34063202321 is IN_PROGRESS at this handoff, not passing evidence. Previous
registry source855a144 CI34062195057 completed SUCCESS. The final checkpoint11
targeted Windowsamd64 race run also PASS3.748s at8c2584b.

Master explicitly ordered HOLD after this coherent checkpoint because the
separate Git foundation review ended with a cybersecurity content/access block.
That independent gate remains unresolved. No alternate/rephrased review or
retained-publication implementation was begun; only final verification and this
documentation followed. Resume affected implementation only after Master resolves
the supported access/review boundary and authorizes resumption.

All11 GitFacts methods and ReleasePlan exist. Common native mutation coordination
is implemented privately; none of eight Prepare methods, ExecutePrepared, Fetch
or Reconcile is implemented yet. This is a partial contribution, never adapter or
Slice acceptance. Current author resumed directly from pushed855a144, with
separate facts/foundation review coordinated by Master on that immutable source.

Checkpoint11 adds a64-call in-process scheduler keyed by LocalCommon, including
bounded canceled waiters, and a permanent common-directory coordination file.
Native Windows LockFileEx/Unix flock owns one exclusive nonblocking OS lock;
Windows also denies delete sharing. It is never truncated/unlinked, age-cleaned
or adopted as a Git ref/index lock. Unknown/unfinished marker files refuse.
Closing/crash releases only the acquired OS ownership. Replacement detection
retains both objects; failed acquisition retains separately inspectable created/
cleanup facts for forthcoming mutation result assembly. No Prepare owns this guard.

Targeted Windowsamd64 and386 tests PASS, including actual SHA1/SHA256 linked
worktrees, separate adapters, clone independence, cancellation, bounded scheduler,
cross-process contention and killed-owner recovery. Windowsamd64 targeted race
PASS; full Git suite PASS55.806s and vet PASS. All12 Git package cross-builds PASS.
Windows no-delete-share replacement control PASS; symlink creation is explicitly
skipped for unavailable local privilege. Native WSL openSUSE-Leap-15.5 ordinary
UID/GID65534 on /tmp ext4 passes scheduler, flock, killed-owner, replacement/no-loss,
foreign-marker and symlink controls. Final native fixture/binary remains at
/tmp/gh-tree-git-scope.AuDwUN; Windows crosscompiled input is
C:/Users/hanse/.codex/tmp/gh-tree-git-scope.test. The earlier broader WSL run passed
those lock controls but failed linked fixtures in existing ResolveLocal under
system Git2.35.3; this is retained as an explicit compatibility/evidence limitation,
not Git2.43 mutation proof. Source CI/independent review remains required.

Remaining work after the HOLD is cleared: actual immutable operation preflight and G8 retained
publication, then full G8a native hook/signing/HEAD-first commit, G9 attachment,
G10 exact stash journals/drop, Fetch/Reconcile and approved continuations. Keep
coherent targeted-verified milestone commits pushed before the next substantial
step. Native Windows/Linux/macOS/ARM64/FreeBSD requirements and complete source
review/CI remain; full Application/State/View/vertical proof remains M4..M8.
Master alone decides acceptance and serial integration. No integration/tag/PR is
authorized from this partial checkpoint.

## Historical checkpoint evidence

Checkpoint1: private transport and physical-directory observations. Source is
adapter.go, transport.go and selected platform/stamp files. Meaningful native
controls in transport_test.go/native_test.go prove bounded separate output,
literal argv, cancel-before-start, canceled-root reaping, explicit descendant pipe
residual, replacement identity and unchanged actual Git index during status reads.
Local Windows/amd64 Go1.25.0/Git2.43.0.windows.1: `go test ./internal/git -count=1
-v` PASS, including SHA-1/SHA-256. `go build ./internal/git` PASS for all12
accepted targets with CGO_ENABLED=0. Native Windows/amd64 architecture checker
PASS; `git diff --check` PASS. Cross-builds are not native platform proof.
This is partial V-GIT-01/09
infrastructure evidence, not proof of full V-GIT-01..10 or supported mutations.

Checkpoint2: ResolveLocal/ListWorktrees now expose actual physically bound common/
administrative identity, native capabilities, primary/current/linked availability,
Attached/Detached/Unborn, occupancy and sanitized versioned remote mappings.
Real SHA-1/SHA-256 tests prove linked equality versus independent clones, retained
missing-root identity/independently readable Head, native locks, commit-stable
identity and remote replacement. Read results preserve partial observations.
Shared native DirectoryIdentity encoding and Remote token are coordinated with
Master/Persistence/GitHub within existing B3/Cwd contracts, without adapter imports.

Full Windows FileIdInfo initially failed with an unaligned byte buffer; aligned
native structure corrected it. Actual Windows/amd64 and386 targeted tests PASS,
all12 adapter cross-builds PASS, native amd64 vet/architecture checks PASS. Stable
birth identity is unchanged by child create/rename; replacement changes identity.
No native Unix execution or complete adapter source CI is claimed yet.

Checkpoint3: ReadCommits and MergeBase implement exact native roots/parents,
verbatim commit messages/time offsets, authenticated source-bound continuation,
explicit root/no-common and every criss-cross candidate. Native SHA-1/SHA-256
history/cursor-transplant/copy/no-common/criss-cross tests PASS. Fact transports
deny all native protocols so config drift cannot enable an implicit fetch; current
unproved promisor object-read profile is explicit. The Git2.43 source establishing
implicit acquisition is linked in repository.go; no API/contract change needed.

Checkpoint4: ListRefs and ResolveExact supply native literal branch refs, separate
annotated tag/peeled commit, symbolic targets, mapped cached freshness and strict
exact target/source/configuration checks. Targeted real tests PASS for both object
formats' literal nested refs and tags, changed-ref cursors, exact branch movement,
same-OID wrong-branch locator, remote equal-bytes refusal, legitimate explicit
mapping and changed-configuration refusal. The initial ref fixture omitted required
Kinds and failed constructor admission; corrected fixtures assert construction.
This now implements6/11 facts methods; no mutation port is implemented yet.

Checkpoint5: ListStashes validates native stash parent shape and preserves exact
OID/occurrence/position, raw metadata and descriptive legacy origin. Native Git2.43
files SHA-1/SHA-256 prepend/three-parent tests and SHA256 duplicate A-B-A occurrence
tests PASS. Identical consecutive native stores are no-ops; the original fixture
was corrected to exercise actual A-B-A duplicate entries. Native Git2.48.1 reftable
passes the same controls. Its compatibility HEAD file initially hid real Head;
native read-only pseudo-ref queries corrected that backend path. Test fixture
selection uses explicit environment parameters, never a product-local Git path.

There are now7/11 facts methods, no mutation methods. Status is held only for
Master's concrete BC-CHANGE #67 (missing independent change causes in accepted
API/contract); no worker change to frozen documents/API is made. Other work proceeds.

Checkpoint6: ReadGraph and ReadStashPatch bring facts to9/11 methods. Real graph
continuation preserves exact roots/refs/Heads/parents; full-OID stash patch reads
remain valid after position shift and fixture deletion. SHA-1/SHA-256 staged,
working and untracked views, rename/binary/absent-untracked, file/byte bounds and
tree-attribute selection despite changed live attributes PASS on Git2.43. Native
Windows386 large-uint32 file-bound control and vet PASS. No view labels enter raw
patch bytes. Empty trees are computed natively without writing objects.

Checkpoint7: private acquired-directory/relative regular-file and exclusive
private-file primitives. Actual Windowsamd64/386 NtCreateFile ABI, protected
user/System DACL, existing-destination retention, stale-root replacement and
native junction refusal tests PASS. Symlink creation is explicitly SKIP on local
Windows due unavailable privilege; junction proof is a distinct native case.
Unix uses native descriptor-relative nofollow/exclusive open; cross-build proof
is separate from future native execution. No public mutation or retained publisher
is supplied by this primitive checkpoint; its full gap/late-writer/crash gates
remain pending. All owned files are committed/pushed before further broad work.

Additional actual Linux primitive proof at source6c234b3: Go1.25.0 cross-built
linux/amd64 test executable ran in existing WSL Linux6.18.33.2 as UID65534. All
targeted directory/nofollow/symlink/replacement, literal/bounded/canceled command,
descendant-pipe and SHA-1/SHA-256 unchanged-index controls PASS (native Git2.35.3).
This is a narrower tested read profile, not full Git2.43/adapter acceptance.
Raw command/results/exit0: internal/git/testdata/native-checkpoint7-linux.txt,
SHA256 ABB7E31072858F5F2884C9E2CB9367CC19C95E3DFE0732AD42F6CC365FE590A0.
The first WSL invocation misparsed unquoted dotted test flags; quoting exact argv
corrected the invocation without source changes. Native macOS proof remains pending.

The complete current internal/git suite and native Windowsamd64 architecture check
also PASS at040e775. Follow-up native Windows locator handling supplies explicit
extended-length DOS/UNC paths without case folding or lossy invalid-UTF8 conversion;
actual >320-character Unicode directory/private-file controls PASS on amd64/386.

An additional native Linux UID65534/Git2.35 mechanism control passes: retain an
acquired common-directory descriptor, replace its old repository path with a clone
sharing the old/new objects, then run native expected-OID update-ref through the
inherited descriptor scope. Only the acquired original changes; the replacement
clone remains at its old OID. directory_linux_test.go records the executable case.
This supports future native ref scope binding, not an implemented mutation port.
Git2.43 source preserves explicit gitdir/commondir strings in repository.c/setup.c;
complete supported-version/platform/hook/publication proof remains required.

Root merged reviewed Git1.1.0/#67 API prerequisite4d1b548 into the unchanged Git
tree at775121dd856bced37d8627a1f0615d33612464d9. Combined CI34056687102 failed
the Linux race helper's1s positive deadline and Windowsamd64/ARM64 SDDL spelling
assertions; those failures remain recorded, not accepted or hidden. Correction
removes only test-child GORACE's documented default1000ms exit sleep, preserving
real budgets/race reporting, and validates native ACL ACE masks/types/flags/SID
equality (also on created private files) instead of SDDL spelling. Native alias,
broadened/missing-rights/unprotected ACL and configured-deadline controls added.
Targeted native Windowsamd64 race and386 ACL controls PASS. Corrected source
checkpoint enables CI; current native hosted results remain the next gate.

Correction ee42b1b passed hosted CI34057368746 (all18 applicable jobs/helper skip),
as independently inspected by Master. Checkpoint8 implements ObserveStatus against
accepted #67: required per-cause rows, current index stages/flags, nofollow regular/
link observations and full-content/index/config/inventory versions. Native tests
PASS for SHA-1/SHA-256 staged/unstaged/both and unchanged index bytes/identity/mtime;
rename plus edit/deletion, staged deletion plus present untracked replacement,
intent-to-add, same-size preserved-mtime edit, unborn and native three-stage
conflict; upstream none/equal/ahead/diverged/gone/unresolved retains exact endpoints
and cached freshness. All12 builds and vet PASS. This supplies10/11 facts methods;
general diff and every mutation remain outstanding, with full independent/native
publication/CI acceptance still required.

Root supplied accepted #68 digest accessor/shared native FreeBSD CI by clean merge
d539a73cb615c558eb8a1317543e12a3973b00fc from verified canonicald5eeaac; Git tree
matched held6375083. Checkpoint9 completes all11 GitFacts signatures, with actual
ReadDiff root/selected-parent/pair and versioned index/worktree/Head comparisons.
Targeted native SHA1/SHA256 root/parent/pair, stagedB versus worktreeC, later-source
refusal and max-uint32 parent controls PASS. Physical worktree versions now depend
on present file facts rather than status cause/index fields; index-only staging
preserves them. General diff, status and private native infrastructure remain
one partial adapter contribution; all12 mutation methods/native protocols are next.

Checkpoint10: private bounded plan registry and public ReleasePlan. Atomic one-use
admission, coordinator issuer plus original approval binding,64 operation/byte
budgets, expiry, sequence reservation/order, actual stored verified/uncanceled
predecessor checks, release-during-execution and receipt-construction-error barriers
PASS under native Windowsamd64 race tests. Fixtures are portable registry values,
not native preparation/publication proof. Native locks/resources are absent from
this registry; per-operation target/source/facet validation and the common scope
scheduler still belong to the mutation implementation. Options carry the shared
coordinator ApprovalIssuer; zero authority is read-only. A separate unpredictable
private plan/receipt issuer avoids exposing its lifetime through SourceVersion.

Checkpoint10 handoff recorded: all11 facts methods plus ReleasePlan existed; none of
the eight Prepare methods, ExecutePrepared, Fetch or Reconcile exists yet. No
complete adapter/Slice is claimed. Native common coordination, preparation plans,
all G8 protected publication, G8a hooks/real signing/HEAD-first commit, G9 attachment,
G10 exact stash journal/drop, recovery and full native/platform gates remain.
The fresh worker must read actual source/tests/accepted contracts and preserve the
bounded checkpoint/push policy. Independent facts/foundation review can proceed
from this exact clean checkpoint while the remaining native work gets fresh context.
The complete native Windowsamd64 Git package suite PASS (52.9s), current Windows
architecture gate PASS, all12 Git package cross-builds PASS and registry-specific
Windows race tests PASS. These checks validate this partial source checkpoint only;
the fresh writer must obtain exact current hosted CI and independent review, then
finish all native mutation requirements before Master can accept #61.
