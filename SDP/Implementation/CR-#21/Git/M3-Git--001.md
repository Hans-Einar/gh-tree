# M3 Git contribution — Issue #61

State: PARTIAL CHECKPOINT; no adapter acceptance or completed Slice.
Worker branch: codereview-21/layer-git; base412f33e477cec03cb6eafe7b846c9bcdd02c0a25.
Authority: #21/#61 resumed policy, M3-Adapters--001, accepted M2 source and
Application--Git/BoundaryTypes FROZEN1.0.0. Master dispatch73a4585/ledger57.
Owned: internal/git/ and this bounded report only. CLI still uses legacy code.

Checkpoint1: private transport and physical-directory observations. Source is
adapter.go, transport.go and selected platform/stamp files. Meaningful native
controls in transport_test.go/native_test.go prove bounded separate output,
literal argv, cancel-before-start, canceled-root reaping, explicit descendant pipe
residual, replacement identity and unchanged actual Git index during status reads.
Local Windows/amd64 Go1.25.0/Git2.43.0.windows.1: `go test ./internal/git -count=1
-v` PASS, including SHA-1/SHA-256. `go build ./internal/git` PASS for all12
accepted targets with CGO_ENABLED=0. Native Windows/amd64 architecture checker
PASS; `git diff --check` PASS. Cross-builds are not native platform proof.
This is partial V-GIT-01/09
infrastructure evidence, not proof of full V-GIT-01..10 or supported mutations.

Checkpoint2: ResolveLocal/ListWorktrees now expose actual physically bound common/
administrative identity, native capabilities, primary/current/linked availability,
Attached/Detached/Unborn, occupancy and sanitized versioned remote mappings.
Real SHA-1/SHA-256 tests prove linked equality versus independent clones, retained
missing-root identity/independently readable Head, native locks, commit-stable
identity and remote replacement. Read results preserve partial observations.
Shared native DirectoryIdentity encoding and Remote token are coordinated with
Master/Persistence/GitHub within existing B3/Cwd contracts, without adapter imports.

Full Windows FileIdInfo initially failed with an unaligned byte buffer; aligned
native structure corrected it. Actual Windows/amd64 and386 targeted tests PASS,
all12 adapter cross-builds PASS, native amd64 vet/architecture checks PASS. Stable
birth identity is unchanged by child create/rename; replacement changes identity.
No native Unix execution or complete adapter source CI is claimed yet.

Checkpoint3: ReadCommits and MergeBase implement exact native roots/parents,
verbatim commit messages/time offsets, authenticated source-bound continuation,
explicit root/no-common and every criss-cross candidate. Native SHA-1/SHA-256
history/cursor-transplant/copy/no-common/criss-cross tests PASS. Fact transports
deny all native protocols so config drift cannot enable an implicit fetch; current
unproved promisor object-read profile is explicit. The Git2.43 source establishing
implicit acquisition is linked in repository.go; no API/contract change needed.

Checkpoint4: ListRefs and ResolveExact supply native literal branch refs, separate
annotated tag/peeled commit, symbolic targets, mapped cached freshness and strict
exact target/source/configuration checks. Targeted real tests PASS for both object
formats' literal nested refs and tags, changed-ref cursors, exact branch movement,
same-OID wrong-branch locator, remote equal-bytes refusal, legitimate explicit
mapping and changed-configuration refusal. The initial ref fixture omitted required
Kinds and failed constructor admission; corrected fixtures assert construction.
This now implements6/11 facts methods; no mutation port is implemented yet.

Checkpoint5: ListStashes validates native stash parent shape and preserves exact
OID/occurrence/position, raw metadata and descriptive legacy origin. Native Git2.43
files SHA-1/SHA-256 prepend/three-parent tests and SHA256 duplicate A-B-A occurrence
tests PASS. Identical consecutive native stores are no-ops; the original fixture
was corrected to exercise actual A-B-A duplicate entries. Native Git2.48.1 reftable
passes the same controls. Its compatibility HEAD file initially hid real Head;
native read-only pseudo-ref queries corrected that backend path. Test fixture
selection uses explicit environment parameters, never a product-local Git path.

There are now7/11 facts methods, no mutation methods. Status is held only for
Master's concrete BC-CHANGE #67 (missing independent change causes in accepted
API/contract); no worker change to frozen documents/API is made. Other work proceeds.

Checkpoint6: ReadGraph and ReadStashPatch bring facts to9/11 methods. Real graph
continuation preserves exact roots/refs/Heads/parents; full-OID stash patch reads
remain valid after position shift and fixture deletion. SHA-1/SHA-256 staged,
working and untracked views, rename/binary/absent-untracked, file/byte bounds and
tree-attribute selection despite changed live attributes PASS on Git2.43. Native
Windows386 large-uint32 file-bound control and vet PASS. No view labels enter raw
patch bytes. Empty trees are computed natively without writing objects.

Checkpoint7: private acquired-directory/relative regular-file and exclusive
private-file primitives. Actual Windowsamd64/386 NtCreateFile ABI, protected
user/System DACL, existing-destination retention, stale-root replacement and
native junction refusal tests PASS. Symlink creation is explicitly SKIP on local
Windows due unavailable privilege; junction proof is a distinct native case.
Unix uses native descriptor-relative nofollow/exclusive open; cross-build proof
is separate from future native execution. No public mutation or retained publisher
is supplied by this primitive checkpoint; its full gap/late-writer/crash gates
remain pending. All owned files are committed/pushed before further broad work.

Next: revised status/general diff after #67 refreeze, native retained publication
and complete guarded mutation plans/receipts;
then scoped scheduling/plans/continuations and complete G8/G8a/G9/G10 native
preparation, protected publication, hook/signing and stash protocols. No permanent
unsupported placeholder substitutes for a required port. No integration/tag/PR
is authorized from this checkpoint. Independent complete-source review, exact CI,
Windows/Linux/macOS/native adverse profiles and all12 builds remain required.
Full Application/State/View/vertical proof remains M4..M8.
