# M3 Persistence admission, fault and resource review

Disposition: **ACCEPT the bounded P3k admission/fault/resource delta. No findings requiring correction in this scope.** This is not complete #63 adapter, native-profile, Slice, integration or release acceptance.

Fresh independent reviewer `m3_persistence_fault_resource_review`, 2026-09-07; authority #63 under #21, Sprint-004-v04 / I-03 / M3, Master dispatch `73a8005f4a0fc366d359ff9f33705321978eb8cd` / ledger 97. Reviewed clean branch `codereview-21/layer-persistence` at **`3772fafc903ccb153a8604230b4143b7e7433e0f`**, worktree `C:/Users/hanse/GIT/gh-tree-wt/persistence-implementation`. Executable/test source is **`ff40e32dcebf36c7182f757b0bc5cb2bdfe08cf8`**: the later checkpoint changes only README and author report. Product corrections are `faec6489f1988dd881269d6985cafd0fa14ee57c` and `75137a0ecc624559efe2a3c61c0236f9c5155e4e`; ff40e32 corrects only Unix test-counter portability and reporting. The bounded comparison against accepted `53de3e1daea2c7ffd0fe6c6bdf365a8c2160357f` contains eleven Go source/test paths, README and author report.

Read AGENTS/developmentInstructions, full #21/#63 including comments, relevant frozen Application--Persistence/BoundaryTypes/BCFreeze and accepted Storage/Verification authority, local README, actual current source/tests and earlier protocol/identity reviews. `SDP/Instructions` is absent; developmentInstructions is operative. Author summaries were navigation only. The existing accepted Windows identity correction at bfdc79c/report53de3e1 and independently confirmed M363-PR01/02/03 at91acac5 remain distinct prior evidence. This review does not reopen unchanged codecs/native identity/security architecture or replace those decisions.

## Source assessment

- `preparation.go:69` separates observation from configured write admission. A smaller record/byte allowance adds `errRecoveryCapacity` while measurement continues. All returned matching names remain available to the existing independent manifest observer. Native open/size/close errors survive joining, aggregate size saturates instead of wrapping, and cancellation preserves earlier errors. Observation stops explicitly at 1280 matching names or after the 16384-entry directory budget, returning an error rather than pretending completeness. It does not promise exhaustive recovery in a directory exceeding those independent bounds.
- `commit.go:314` observes every returned manifest before handling inventory failure. The existing capacity reservation at lines340/341 still precedes new original/payload/manifest preparation. `store.go` retains an independently valid current document/version beside recovery errors. The real lower-budget tests compare complete immutable records and IDs, document/current/proposed facts, and every existing filename/byte. No eviction, replay or preparation-at-capacity was introduced.
- `manifest_journal.go:61/69` places private request checkpoints immediately before frame write and flush. The callback uses the current synchronous preparation stage; no public callback or background owner is added. `commit.go:534` gives the final ready snapshot its own stage. A process killed at a pre-flush checkpoint may leave readable, unflushed bytes; that observation is not proof of crash durability or publication causality.
- `commit.go:44/157` attempts native close before delivering an injected close failure, joins the native error, and records CleanupIncomplete for each failed artifact/lock/parent-chain close. One failure does not skip subsequent cleanup. The precommit close removes the object from the owned list before closing it, then returns on failure so remaining resources are closed by the request defer. Native parent-chain and lock close implementations also attempt all owned closes/unlock operations. Current observation occurs while the lock is retained; close diagnostics and cancellation remain separate from publication/current/proposed facts.
- The 35-stage table produces 70 failure/cancellation cases, retains the actual protocol, and disables automatic GC around exact native resource accounting. Its close stages instrument close-result delivery; `close.parents` represents the complete chain, not a separate injected syscall for each ancestor. Windows counts current-process native File-type handles, including directory guards and locks, with the previously accepted duplicate/directory/lock negative controls. Unix counts names while retaining its enumeration descriptor and proves a deliberate open adds exactly one. This counter avoids macOS `os.ReadDir`'s transient-entry `lstat` problem. The measurements establish exercised file-resource ownership, not every process/thread/event resource or arbitrary native-close failure behavior.
- Real refusal controls remain separate: Windows sharing violation; six ordinary-user Unix EACCES creation/retention/publication cases with retained-descriptor mode restoration; a joined child limited to a real 40000-byte partial payload; and twelve killed-process boundaries with exact recovery counts and repeated stable records. No native primitive, original identity, permission profile or frozen interface changes.

## Independent native verification

Direct Go1.25.0 from the existing Windows toolchain; CGO_ENABLED=0, GOTOOLCHAIN=local. Native Windows11 build26200 / NTFS; Linux6.18.33.2 WSL x86_64 on verified ext4, UID/GID65534. Linux executes a crosscompiled test binary natively from owned `/tmp/gh-tree-independent-faults.Aqy4P4`, not from `/mnt`. Test-only additive overlays do not replace any existing product/test file. Commands, exit statuses, binary/evidence hashes and compact logs are in [EvidenceManifest.json](../../../Verification/CR-%2321/Evidence/M3-Fault-Resource-Review--001/EvidenceManifest.json).

| Control | Observed result |
|---|---|
| Frozen-source 70 fault/cancellation cases, lower-admission preservation, actual sharing refusal, twelve process-death boundaries, existing handle-count negative control; additional reviewer tests | Windows amd64 PASS, package5.758s; native386/WOW64 PASS, package6.419s. All subprocesses joined and expected counts/records checked. |
| Same applicable fault/admission/death controls, all six actual EACCES stages, actual partial-write child, additional reviewer tests | Ordinary-user Linux/ext4 PASS, total execution4.748s. The child fixture's direct top-level skip is its entry guard; the parent really executed it and checked the success marker and restart facts. |
| Independent four simultaneous close-delivery failures, plus cancellation, both before and after publication | PASS on amd64/386/Linux with GC disabled. Every distinct error and per-close CleanupIncomplete diagnostic survives; exact handle/descriptor baseline is restored. Before-publication error remains identifiable; current/proposed/recovery records and known outcome are preserved. Fresh lock/load succeeds. |
| Independent hard matching-name bound | PASS on amd64/386/Linux:1281 real matching files return1280 measured names and explicit capacity refusal; all1281 remain. |
| Independent combined native ELOOP and lowered record/byte capacity | Linux PASS: all11 exact baseline recovery records remain with usable current document/version, proposed version, both errors and unchanged prior names/bytes; unsupported symlink remains untouched and no preparation begins. The observed first absent Unix Linkat publication retains its extra owned name, so this baseline is11 rather than the Windows10. |
| Independent hard directory-entry bound | Linux PASS:16385 unrelated entries cause explicit bounded refusal, with no entry changed or removed. |
| Real40000-byte partial-write child with `GOGC=off` inherited from process startup | Linux PASS: NotCommitted, original/manifest retained, exact descriptor count restored, child joined, fresh restart retains independently proved facts. |

These results independently confirm the bounded correction and meaningful fault/resource evidence. The small overlays and logs are retained under the existing SDP verification tree; full CI logs, runner metadata and the Linux binary remain in `C:/Users/hanse/.codex/tmp/m3-fault-resource-review-20260907`. No duplicate source checkout, cache or binary archive was added.

## Exact CI and retained gates

Independently queried [CI34080421099](https://github.com/Hans-Einar/gh-tree/actions/runs/34080421099) at exact ff40e32: **18 SUCCESS, native FreeBSD FAILURE, Runtime helper SKIPPED**. Native Windows amd64/ARM64, macOS ARM64, Linux, Linux race and all twelve architecture jobs succeed. Reopened [Windows ARM64 job101614565164](https://github.com/Hans-Einar/gh-tree/actions/runs/34080421099/job/101614565164): actual Windows/ARM64 host, GOHOSTARCH/GOARCH arm64, Go1.25.0 windows/arm64, Persistence17.120s, full tests/vet/build. Reopened [macOS job101614565186](https://github.com/Hans-Einar/gh-tree/actions/runs/34080421099/job/101614565186): actual macOS26 ARM64, Go1.25.0 darwin/arm64, Persistence5.743s, full tests/vet/build. These are execution facts, distinct from cross-builds.

The [FreeBSD job101614565392](https://github.com/Hans-Einar/gh-tree/actions/runs/34080421099/job/101614565392) still executes as ordinary UID/GID1001 and records user-namespace success versus system-namespace `Operation not permitted`. The aggregate run fails. Prior75137a0 [CI34080235390](https://github.com/Hans-Einar/gh-tree/actions/runs/34080235390) is canceled, with macOS fixture failure, FreeBSD failure, ARM64 cancellation and helper skip; that history is preserved and not treated as passing evidence.

**Full #63 remains open** for the ordinary-user FreeBSD system-EA/profile decision and its native verification, no-birth persistent incarnation association, and the Unix final source-name-check/name-syscall interval. The source interval remains distinct from the accepted arbitrary target-editor comparison/publication gap. No privilege/helper, EPERM-as-empty, metadata loss, read-only mode, supported-target reduction, unreviewed mechanism or waived guarantee is accepted. Injected delivery/close/flush-boundary failures and process death are not physical device-flush or power-loss proof. All six Storage methods, unaffected native/full-adapter checks, downstream Application/vertical/full-integrated verification and release gates remain required.

Reviewer changed only this report and its small evidence folder. The reviewed Persistence tree is `8cc553a579d373754aab1d4ad9aff9d02e1049fd`; product/test/frozen/workflow/module files stayed unchanged. Report/evidence are committed and pushed separately with `[skip ci]`; clean scope and remote equality are checked at handoff. No source merge, integration or full Slice/baseline finding closure occurs. The separate blocked Git review/access/model controls and preserved denied-ACL fixtures were untouched.

Exact next permitted action: Master may record this bounded acceptance and preserve the evidence, then continue the remaining explicitly authorized #63 work subject to its unresolved authority/native gates. This acceptance does not authorize complete adapter acceptance or serial integration past prior gates.
