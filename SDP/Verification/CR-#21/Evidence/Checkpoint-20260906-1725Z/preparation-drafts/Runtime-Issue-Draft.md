# DRAFT — not active implementation authority

Parent #21; M3 Runtime implementation after complete independently accepted/integrated M2 surfaces and exact canonical CI. Master records actual kickoff and predecessor authority. Read mandatory governance/full #21/this Issue, accepted Runtime #25 and related State/Discovery/Composition reviews, REFDES/API/CwdAcquisition/WindowsBroker, Runtime feasibility RTF-02 superseding numeric signaling, MigrationMap/Slices/Verification/FindingDisposition, complete frozen Application--Runtime/shared BoundaryTypes/related BCs/BCFreeze, actual M2 values/ports, and accepted M1 helper CI interface. Design prototypes are not product proof.

Fresh worker owns new internal/runtime/ including broker/broker-cmd/brokerassets/cmd/helpergen/private supervisors/helpers/platform transports/tests/README and SDP/Implementation/CR-#21/Runtime/M3-Runtime--001.md, branch codereview-21/layer-runtime in its dedicated worktree. No legacy process/provider/terminal moves, other concrete adapters, Application/State/View/host/cmd cutover, module/workflow/Master metadata or frozen record changes. Composition coordinates CI/private-mode host dispatch later; Runtime supplies its bounded interface/test harness. Master assigns a separate fresh reviewer. No product PR/integration/tag/release.

Implement all12 exact Sessions methods with one authoritative launch/interactive registry, nonzero monotonic never-reused/wrapped SessionIDs, membership+allocation+admission synchronization and private immutable start environment/specification. Actual M2 Domain/API/ports must be consumed directly. No Application ID allocation/workflow/provider discovery/storage/Git calls, public native handle/PID/signal/broker DTO or second session registry. Original worktree/cwd never follows later active selection. Start Session absent only before admission; failed admitted starts retain identity/resources/final event; Established means actual full startup barrier, even if immediate natural exit is already Cleaned.

Preserve all lifecycle/operation-neutral snapshots/effects/diagnostics with errors. Stop coalesces through cleanup; only all barriers/residual absence permits Cleaned. Restart deduplicates same operation/input, refuses changed key use/concurrent extra replacement, fully cleans old before fresh allocation, reuses original spec/latest accepted geometry, honors cancel before replacement. Starting/natural-exit/in-flight restart belong to aggregate shutdown. Initial64 live sessions/256 reserved final events,256 cleaned history; cleanup-pending resources never evict. Admission and global/session locks never span OS waits. Budgets2s grace/3s force-join/8s aggregate are reporting bounds, not a promise to clean arbitrary failure.

Implement bounded256KiB raw multi-stream output ring, exact byte offsets/gaps/chunks/sequence and no ANSI/text interpretation; MaxBytes1..262144 and offset validity. Input copies entire nonempty request or none with64KiB outstanding/in-flight bound; AcceptedBytes is queue ownership, not child consumption, and later partial-write facts cannot authorize replay. Positive bounded geometry, capability-defined ETX and accepted/delivered facts remain separate from child behavior. Native output draining and Stop cannot block on absent/slow UI.

One Application event consumer: global sequence and per-session sequence distinct; coalescible hints, one reliable final Cleaned event per admitted session, bounded reservation before acquisition, correct delivered-cursor replay/monotonic cumulative ACK. Snapshot eviction cannot drop unACKed final events. Event stream remains drainable through shutdown; EOF only after closed admission, joined producers and acknowledged reliable finals. Application transfer/ACK integration remains later full-stack proof, but Runtime replay/misuse/capacity rules require independent tests now.

Acquire cwd independently from supplied observation with retained nofollow root/project identities; no check/reopen path, parent cwd mutation or fd pathname substitution. Unix private same-executable SID supervisor Fstats/Fchdirs designated acquired descriptor once and closes it before user command lookup/execution, with exact nonce/role/session handshake. It owns controlling tty and user root in a different PGID. Ordinary job-control cleanup uses helper setpgid-before-exec, self SID/group validation, authenticated Joined→Commit and permitted self-group kill(0), never numeric census precheck/kill. Grace TERM, acquired STOP helpers/full recensus then KILL. Require no live same-SID members except supervisor, joined waiters, Quiescent→Release→supervisor exit/exact wait/all PTY/pipe/control joins. Linux proc, Darwin native census and FreeBSD selected bounded /bin/ps profile; observed escape/unacquirable group/helper/supervisor failures remain residuals.

Windows implements complete WindowsBroker contract: native-machine helper routing including386→native64 and ARM64 emulation, suspended broker assigned outer kill-on-close Job beforeResume, broker-owned inner user Job/HPCON, real data/list-read chain guards/nonempty anchors. One LockOSThread owner starts debugged+suspended user root, innerJob beforeResume, target-ABI initial runtime breakpoint (not arbitrary first event), correct PEB cwd handle duplication/FileIdInfo verification and pending-event anchor release/detach before user initialization. No handle injection/process-memory writes or metadata-only substitution. Unknown ABI/profile/partial acquisition refuses with exact cleanup; 30s startup bound. Framed64KiB nonce/sequence-bound closed control, explicit output-reader handle transfer, broker-owned input/resize/close. InnerJob0+rootwait+IO quiescence→Release→broker exit→outer residual terminate/Job0+parent joins; no outer==broker-beforeRelease circular wait. Every native goroutine/debug/DLL/pipe/job/extraction resource owns immediate unwind and must actually join.

Implement deterministic embedded native helper assets with the accepted M1 interface exactly: go run ./internal/runtime/cmd/helpergen -check on Windows/amd64 Go1.25.0; brokerassets/broker-amd64.gz, broker-arm64.gz, manifest.json and broker/broker-cmd/helpergen source closure. -check truly independently rebuilds/source-closure/PE/compression/hash verifies without rewriting checkout. Pinned modules, CGO0, explicit target/microarchitecture, trimpath/buildvcsfalse/emptybuildid and deterministic gzip; no sourceSHA self-reference/recursive embedding. Ordinary clean builds need no generation/download/compiler and retain12 public assets. Extraction uses exclusive nonce/protected current-user+SYSTEM ACL/no-reparse/create-new/fullwrite+flush/read-only image guard/FileIdInfo/hash/machine/protocol; cleanup only exact owned image/empty directories after outer cleanup, unexpected entries remain residual. First Runtime Go/image input makes missing M1 conformance prerequisites fail.

Verification must satisfy V-RUN-01..08 and related Domain/App/Discovery/Composition/Slice clauses at exact source: real native Windowsamd64/ARM64/WOW64/emulation, Linux/macOS/FreeBSD, actual PTY/ConPTY startup/input/resize/blocking-close/job-control/child-grandchild identities; bounded simultaneous Stop/Restart/Shutdown, root exits first, stopped/background groups, helper failure/escape, source/root/cwd reparse/substitution at acquisition barriers, target ABI/DLL/TLS/debug-heap/immediate-chdir compatibility, injected native partial failures and resource counts. All12 builds/layout assertions/helper repro are distinct from native behavior. No root-killed or wrapper-success cleanup inference, no omitted platform proof hidden as skip. Request Master-coordinated CI scope where needed; read-only FreeBSD action source preparation is available but no guest execution has yet occurred.

Freeze/push complete source, generated assets and real local/CI/native evidence/limits. Separate fresh reviewer inspects actual source/contracts/tests/artifacts/exactSHA and meaningful adverse/positive controls; correct/re-review before acceptance. Master serially integrates Runtime after Git→GitHub→Persistence→Discovery, verifies integrated CI and records source/review/integration/asset evidence. No full Slice/baseline/native-release completion from a registry or cross-build alone. Insufficient frozen contract requires BC-CHANGE/refreeze/reverification, not a local fallback.
