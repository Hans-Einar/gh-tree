# Independent bounded Runtime / Storage successor review

Disposition: **BOUNDED ACCEPT** at frozen6e289c4b47a7816493f376c886c00bf0df4835e8, parent246e2a6b02e6812cc7579770e5aac973e19b3073, Issue #59 under #21. The bounded Runtime portion of M259-M05 and the reviewed M259-M06/M07 cases are corrected. No remaining concrete finding was established in this assigned scope. This is not whole-contribution, native-adapter, Slice, CI, integration or release acceptance; primary Reviewer and Root own those gates.

## Source and authority

Reviewed actual changed Runtime, Storage, effect and recovery-subject consistency helpers, their constructor call sites, ports/storage.go, and changed local regression tests. Reopened relevant FROZEN1.0.0 Runtime/Persistence/BoundaryTypes clauses; Git comparison verifies those contracts and mandatory root AGENTS.md/developmentInstructions.md are unchanged from the preceding exact reviewed candidate. Full authority and earlier reports remain #59/#21 and the prior bounded review chain. No author summary was used as evidence.

Created a fresh owned snapshot via `git -c core.autocrlf=false archive` at exact6e289c4. All77 archived original files, including API/ports/Domain source/tests, module files and copied governance/contracts, match exact Git blob IDs byte-for-byte; source-copy-hashes.json records each hash and source-copy-verification.json records zero mismatches. Actual worktree HEAD stayed6e289c4 during the review. Its only dirty file was worker-owned SDP/Implementation/CR-#21/Application/M2-API--001.md. No product or finalized246e evidence file was edited, and no parallel State source was inspected.

## Corrections independently verified

- **M259-M06 compatible subject assertions:** recovery_subject_consistency.go:5-44 merges compatible Optional values into a validation-only subject and reruns the shared subject validator. storage_consistency.go:75-82 retains that accumulated subject through every subsequent record. The returned StorageRecovery records remain the originally supplied immutable values. Both record orders now accept the formerly rejected complementary optional Repository case; tests compare each original RecoveryID, full shared record and optional presence after construction. Conflicting store/worktree/root/family bindings still refuse. Four added three-record tests supply a known Revision, Branch, Stash or Session assertion, an intervening record omitting it, and a later contradictory assertion: both orders refuse in load and commit results. Equal assertions across the gap remain valid. Distinct artifact IDs stay distinct; a repeated ID with altered optional content is still inconsistent, while equal repeated records normalize to one ID.
- **M259-M05 Runtime outcome/effect supplement:** runtime_consistency.go:12-16 applies effect_consistency.go:61-80 to an established Start's RuntimeResources facts. The exact prior Running/Established=true plus sole NotStarted or VerifiedNoTargetChange inputs now return errors and invalid zero values. Empty/unasserted, AppliedVerified, Partial, Indeterminate, mixed AppliedVerified+NotStarted and mixed AppliedVerified+VerifiedNoTargetChange reports remain valid with their original facts retained. A report containing only the two no-effect states refuses. This validates explicitly supplied factual consistency without inventing missing native proof or ordinally reducing independent step facts.
- **M259-M07 retained corrections:** all original start identity/establishment and restart worktree/cwd mismatches still refuse. Present replacement requires an admitted Session and a newer ID; same/self/older identity and failed old cleanup refuse. Cancellation before new admission, admitted failed replacement, failed acquisition without acquired cwd, immediately Cleaned establishment, geometry changes and refreshed source tokens remain representable. Original restart worktree/root/project/executable/argv/terminal continuity and acquired cwd subject checks hold.

The contract basis remains BoundaryTypes B1 optional absence and copy rules, B5 independent effect/recovery facts and duplicate-ID semantics; Persistence BC:116-117, :278-298, :470-495; Runtime BC:197-206, :227-236, :292-322 and :484-489. The original six malformed cases are replayed with the same semantic inputs; where a shared nested constructor now refuses earlier, the probe records that earlier refusal rather than constructing an impossible outer value.

## Retained positive and negative coverage

The five independent probe files replay all original four positive/control groups and additional cases: complete run binding versus differing content bytes; distinct document families and SourceVersion/StorageVersion domains; corrupted/documentless loads with independent recovery, missing current versions and unknown roots; committed-with-error/cancellation and complete StorageRecovery normalization; valid same-store absence/presence; all declared known Null and unknown JSON retention/copy/depth/UTF8/duplicate controls; bounded input/output/event identity and sequence controls; aggregate cleanup refusal with pending sessions; failed admitted and immediately Cleaned starts; all accepted restart failure, cancellation, identity, spec and geometry/freshness variants. No control required a native I/O, registry, process or publication claim from pure value constructors.

The earlier expected-absence anchor interpretation remains **not a finding**. The original Expected anchor token is retained in RunConfigCommit, while RecoveryRecord.Original can describe the guarded prepublication absence after the actual parent is established; Proposed and result versions bind that actual parent. Persistence BC:278-288 and :392-394 separately require preserving the input Expected token in the native manifest. The positive constructor path for that distinction passes. Rejecting an arbitrary anchor-as-Original/actual-parent-as-Proposed mismatch does not justify API broadening.

## Independent execution evidence

- `independent-unit.log` / `independent-unit-result.json`: direct Windows Go1.25.0, `go test ./internal/application/api -run '^(TestRS|TestRC)' -count=1 -v`, **PASS, exit0**.
- `independent-race.log` / `independent-race-result.json`: same owned exact snapshot and probe selection with `-race`, **PASS, exit0**.
- `mutant-reset-subject.log` / `mutant-reset-subject-result.json`: Go overlay changes only the validation accumulator assignment to forget earlier optional assertions. The four Revision/Branch/Stash/Session gap controls all fail as intended, **exit1**. The original source files are untouched and their blob verification remains exact. The baseline for these same controls passes. This establishes that the tests detect the specific skipped-record regression rather than merely exercising a happy path.

Toolchain: C:/Users/hanse/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.0.windows-amd64/bin/go.exe. Initial reviewer fixture compilation reversed the first two NewBranchID arguments; fixture-build-initial.log/result.json preserve that isolated error, corrected before all final executions. It was not a product finding.

Evidence root: C:/Users/hanse/GIT/gh-tree-review-evidence/m2-runtime-storage-6e289c4. `exact-source.tar`, the77 original-file hash records, final logs/results, mutation source/overlay and the five `snapshot/internal/application/api/review_*_test.go` probes are all listed with SHA256 in evidence-manifest.json. Earlier finalized246e report and supplementary manifest remain untouched.

Exact next permitted action: primary Reviewer consolidates this bounded ACCEPT with its independent complete contribution disposition. Root must verify the report-only successor preserves reviewed product blobs, check current configured exact-head CI and perform any authorized integration/traceability gate. This review does not substitute the accepted pure-value evidence for future Runtime/Persistence/native/vertical verification.
